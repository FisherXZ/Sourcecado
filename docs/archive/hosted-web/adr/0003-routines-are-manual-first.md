# Routines Are Manual First

Sourcecado Routines are saved, repeatable sourcing processes, but the committed two-month roadmap will run them manually before adding automatic scheduling. This preserves the product value of reusable sourcing instructions and consistent run outputs while avoiding the extra risk of unattended Apollo credit use, expired auth, background workers, retries, and notification policy.
