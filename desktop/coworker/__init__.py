"""Club sidecar — the local brain.

Slice 11: persona markdown, Gmail draft (ask, never send), scheduler stub, Warm Operator, Apollo.
Copied shape: OpenWorker `openworker-server` binds 127.0.0.1 and requires a launch token.
"""
